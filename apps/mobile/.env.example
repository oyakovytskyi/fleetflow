# Copy to `.env` (gitignored) and adjust.
#
# A physical device cannot reach `localhost` — use the LAN IP of the machine
# running the API, e.g. http://192.168.0.10:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
