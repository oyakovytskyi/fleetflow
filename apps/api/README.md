# API (FastAPI)

Target layout:

```
app/
  main.py
  api/           # routers
  models/
  schemas/
  services/
  repositories/
  websocket/
  core/          # config, security, deps
tests/
requirements.txt
```

Generate next with a minimal FastAPI app + docker-compose (postgres, redis).
