# Admin (Next.js)

Placeholder. Generate with:

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app
```

## Routes (target)

- `/login`
- `/dashboard`
- `/drivers`
- `/deliveries`
- `/tracking`
- `/driver/[id]`
